spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sb-pg
    username: admin
    password: 1234
    name: sb-pg
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
  security:
    user:
      name: admin
      password: p@$$w0rd
